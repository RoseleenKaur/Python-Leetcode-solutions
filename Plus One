class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        st=''
        for i in digits:
            st+=str(i)
            
        num=int(st)+1
        st=str(num)
        return [x for x in st]
